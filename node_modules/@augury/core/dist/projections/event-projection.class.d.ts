import { AuguryEvent } from '../events';
import { Projection } from './projection.interface';
export declare abstract class EventProjection<Result> implements Projection<AuguryEvent, Result> {
    abstract process(event: AuguryEvent): boolean;
    collectResult(): Result | null;
    protected abstract getResult(): Result | null;
    protected abstract cleanup(): any;
}
//# sourceMappingURL=event-projection.class.d.ts.map