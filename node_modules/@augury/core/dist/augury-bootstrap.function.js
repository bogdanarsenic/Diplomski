import { AuguryCore } from './augury-core.class';
import { defaultProbes } from './probes';
export function auguryBootstrap({ platform, ngModule, NgZone, plugins, }) {
    const ngZone = new NgZone({ enableLongStackTrace: true });
    window.augury = new AuguryCore(defaultProbes, plugins, ngZone, ngModule);
    return platform().bootstrapModule(ngModule, { ngZone });
}
//# sourceMappingURL=augury-bootstrap.function.js.map