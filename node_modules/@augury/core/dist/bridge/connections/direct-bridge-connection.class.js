import { EventEmitter } from '../../events/event-emitters';
export class DirectBridgeConnection {
    constructor(messages = new EventEmitter()) {
        this.messages = messages;
    }
    send(message) {
        this.messages.emit(message);
    }
    listen(handleIncomingMessage) {
        return this.messages.subscribe(handleIncomingMessage);
    }
}
//# sourceMappingURL=direct-bridge-connection.class.js.map