import { EventEmitter, Subscription } from '../../events/event-emitters';
import { BridgeConnection } from '../bridge-connection.interface';
import { BridgeMessage } from '../bridge-message.interface';
export declare class DirectBridgeConnection implements BridgeConnection<BridgeMessage> {
    messages: EventEmitter<BridgeMessage<any>>;
    constructor(messages?: EventEmitter<BridgeMessage<any>>);
    send(message: BridgeMessage): void;
    listen(handleIncomingMessage: (message: BridgeMessage) => void): Subscription;
}
//# sourceMappingURL=direct-bridge-connection.class.d.ts.map