function messageMatchesType(message, types) {
    return types.includes(message.type);
}
export function isTimelineMessage(message) {
    return messageMatchesType(message, ['task', 'instability-period', 'change-detection']);
}
export function isDragMessage(message) {
    return messageMatchesType(message, ['drag']);
}
//# sourceMappingURL=bridge-message.functions.js.map