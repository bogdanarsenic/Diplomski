export class Bridge {
    constructor(connection) {
        this.connection = connection;
    }
    send(message) {
        this.connection.send(message);
    }
    listen(handleIncomingMessage) {
        return this.connection.listen(handleIncomingMessage);
    }
}
//# sourceMappingURL=bridge.class.js.map