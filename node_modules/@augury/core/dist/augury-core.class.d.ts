import { Subscription } from './events/event-emitters';
import { Plugin } from './plugins';
import { Probe } from './probes';
import { EventProjection } from './projections';
export declare class AuguryCore {
    private readonly probeManager;
    private readonly historyManager;
    private readonly pluginManager;
    constructor(probes: Probe[], plugins: Plugin[], ngZone: any, ngModule: any);
    registerEventProjection<Result>(projection: EventProjection<Result>, handleResult: (result: Result) => void): Subscription;
    projectFirstResultFromHistory<Result>(projection: EventProjection<Result>, startEventId?: number, endEventId?: number): Result;
    projectResultsFromHistory<Result>(projection: EventProjection<Result>, maxResults?: number, startEventId?: number, endEventId?: number): Result[];
}
//# sourceMappingURL=augury-core.class.d.ts.map