import { AuguryEvent } from '../events';
import { ProbeManager } from './probe-manager';
export declare abstract class Probe {
    private probeManager;
    initialize(probeManager: ProbeManager, ngZone: any, ngModule: any): void;
    abstract doInitialize(ngZone: any, ngModule: any): any;
    emit(createEvent: () => AuguryEvent): void;
}
//# sourceMappingURL=probe.class.d.ts.map