import { AuguryEvent } from '../events';
import { EventEmitter } from '../events/event-emitters';
import { Probe } from './probe.class';
export declare class ProbeManager extends EventEmitter<AuguryEvent> {
    private readonly probes;
    constructor(probes: Probe[], ngZone: any, ngModule: any);
    private initializeProbes;
}
//# sourceMappingURL=probe-manager.d.ts.map