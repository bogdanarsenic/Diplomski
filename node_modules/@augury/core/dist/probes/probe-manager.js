import { EventEmitter } from '../events/event-emitters';
export class ProbeManager extends EventEmitter {
    constructor(probes, ngZone, ngModule) {
        super();
        this.probes = this.initializeProbes(probes, ngZone, ngModule);
    }
    initializeProbes(probes, ngZone, ngModule) {
        return probes.reduce((probesMap, probe) => {
            probe.initialize(this, ngZone, ngModule);
            probesMap.set(probe.constructor.name, probe);
            return probesMap;
        }, new Map());
    }
}
//# sourceMappingURL=probe-manager.js.map