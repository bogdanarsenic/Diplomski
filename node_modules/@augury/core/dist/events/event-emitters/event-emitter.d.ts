import { Emittable } from './emittable.interface';
import { Subscribable } from './subscribable.interface';
import { Subscription } from './subscription.interface';
export declare class EventEmitter<EventType> implements Emittable<EventType>, Subscribable<EventType> {
    private subscribers;
    subscribe(handleEvent: (event: EventType) => void): Subscription;
    emit(event: EventType): void;
}
//# sourceMappingURL=event-emitter.d.ts.map