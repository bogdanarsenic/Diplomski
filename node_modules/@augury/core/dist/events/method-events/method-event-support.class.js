import { AuguryEvent } from '../augury-event.class';
export class MethodEventSupport extends AuguryEvent {
    constructor(classType, clazz, instance, methodName) {
        super();
        this.classType = classType;
        this.clazz = clazz;
        this.instance = instance;
        this.methodName = methodName;
        this.timestamp = performance.now();
    }
}
//# sourceMappingURL=method-event-support.class.js.map